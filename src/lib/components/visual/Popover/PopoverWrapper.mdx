---
name: PopoverWrapper
menu: Customize UI
---
import { Playground, Props } from 'docz';
import PopoverWrapper from './PopoverWrapper';

`PopoverWrapper` used to attach a triangle and position `Datepicker` relative to `Input`.

## Props

<Props of={PopoverWrapper} />



