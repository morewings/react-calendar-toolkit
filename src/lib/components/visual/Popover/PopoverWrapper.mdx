---
name: PopoverWrapper
menu: Customize UI
---
import { Playground, Props } from 'docz';
import PopoverWrapper from './PopoverWrapper';

`PopoverWrapper` component  wraps `Datepicker` inside popover. Used in [`DatepickerInput`](/datepicker-input). You can set change background color using `theme` prop. Replace it with custom component.

## Props

<Props of={PopoverWrapper} />



