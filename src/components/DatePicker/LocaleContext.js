import React from 'react';

const LocaleContext = React.createContext({});

export const useLocaleContext = () => {};

// export const withLocaleContext = (WrappedComponent, selectData) => {
//   const Component = props => {
//     return
//   }
//   return
// }

export default LocaleContext;
